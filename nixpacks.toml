[phases.setup]
nixPkgs = ["nodejs_20", "python312"]

[phases.install]
cmds = [
  "npm ci --omit=dev",
  "python3 -m pip install --no-cache-dir -r apimink/fused-backend/requirements.txt"
]

[start]
cmd = "bash start.sh"
