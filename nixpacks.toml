[phases.setup]
aptPkgs = ["python3", "python3-pip", "python3-dev", "build-essential"]

[phases.install]
cmds = [
  "npm ci --omit=dev",
  "pip3 install --no-cache-dir -r apimink/fused-backend/requirements.txt"
]

[start]
cmd = "bash start.sh"
